# Transparent latch vs. dff on the same data.
clk c1 1
wire d  = 01111000
wire en = 11100100

def notrace dff(D, CP) -> Q:
	always(posedge CP):
		return D

def notrace latch(D, E) -> Q:
	always(E):
		return D

wire q_latch = latch(d, E=en)
wire q_dff   = dff(d, c1)
